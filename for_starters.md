# 이번 세션에서 한 일 — 파이썬 초보자를 위한 정리

이 문서는 `seoul-apt-latest.csv` 데이터를 분석해서 개인 웹페이지에 정리하고, GitHub에 올려서 인터넷에 공개하기까지의 전체 과정을 설명합니다. 각 단계가 왜 필요했는지, AI가 무엇을 했는지, 그리고 만약 사람이 직접 했다면 어떤 지식과 작업이 필요했을지를 함께 적었습니다.

---

## 1. 전체 프로세스가 향한 목적

한 문장으로 요약하면: **"CSV 데이터 → 분석 코드 → 분석 결과 → 웹페이지 → 인터넷 배포"** 라는 하나의 파이프라인을 완성하는 것이었습니다.

```
[원본 데이터]        [분석]           [결과 정리]        [배포]
seoul-apt-      →   analyze.py   →   index.html   →   GitHub Pages
latest.csv          base.py          (표, 문단)        (실제 URL)
```

이 파이프라인에는 반드시 거쳐야 하는 단계가 있었고, 각 단계는 다음 단계의 재료를 만드는 역할을 했습니다.

| 단계 | 이름 | 역할 |
|---|---|---|
| 1 | 데이터 탐색 | 파일에 어떤 컬럼이 있는지, 인코딩은 무엇인지 파악 — 이게 틀리면 이후 모든 계산이 틀어짐 |
| 2 | 데이터 분석 (pandas) | 빠르게 통계를 뽑아 "무엇을 보여줄지" 방향을 잡음 |
| 3 | 표준 라이브러리로 재작성 | 외부 패키지 없이도 동작하는 코드로 바꿔 재사용성/이식성 확보 |
| 4 | 분석 로직 확장 | 단순 평균에서 "자치구 간 비교(갭 분석)"로 질문을 구체화 |
| 5 | 웹페이지에 반영 | 분석 결과를 사람이 읽을 수 있는 형태(제목/문단/표)로 번역 |
| 6 | 버전 관리 & 업로드 | 작업물을 GitHub에 안전하게 저장 |
| 7 | 배포 | 아무나 URL로 접속해서 볼 수 있게 공개 |
| 8 | 디자인 시스템 적용 | 정보 전달력을 넘어 "보기 좋은" 페이지로 다듬음 |
| 9 | 기획 (PRD 작성) | 다음 산출물인 "부동산 대시보드"를 만들기 전에, 누구를 위해 무엇을 만들지 인터뷰로 확정 |

각 단계는 이전 단계 없이는 의미가 없습니다. 예를 들어 3번(표준 라이브러리 재작성)이 없었어도 2번의 pandas 코드로 6, 7번까지 갈 수는 있었지만, 이번 세션에서는 "pandas 없이도 짤 수 있는가"라는 학습 목적이 추가되어 별도 단계로 진행됐습니다.

---

## 2. 단계별 상세 — AI가 한 일 vs 사람이 했다면

### 2-1. 데이터 탐색 (인코딩 문제 해결)

**무슨 일이 있었나:** `seoul-apt-latest.csv`를 처음 읽었을 때 터미널에 한글이 `������` 처럼 깨져서 출력됐습니다.

**AI가 한 일과 목적:** 파일이 실제로 깨진 것인지, 아니면 터미널이 한글을 잘못 표시하는 것인지 구분하기 위해 파일을 바이트 단위로 열어 UTF-8 디코딩이 정상적으로 되는지 확인했습니다. 목적은 "데이터가 손상됐다"는 잘못된 결론을 내리고 엉뚱한 재작업을 하지 않기 위해서였습니다.

**사람이 했다면:** 아래와 같은 지식이 필요합니다.
- 한글 CSV 파일의 흔한 인코딩 3종(UTF-8, UTF-8 with BOM, CP949/EUC-KR)을 알고 있어야 함
- Windows 터미널(특히 Git Bash)의 기본 콘솔 코드페이지가 UTF-8이 아닐 수 있다는 것을 알아야 함
- `open(path, encoding=...)` 을 여러 인코딩으로 바꿔가며 시도하거나, 파일을 바이너리로 열어 `\xea\xb0\x95` 같은 바이트를 직접 눈으로 읽고 UTF-8 표인지 확인하는 경험이 필요
- 결론: 초보자가 가장 많이 막히는 지점 중 하나로, "화면에 깨져 보인다 = 파일이 깨졌다"는 오해를 방지하는 것이 핵심

### 2-2. pandas로 첫 분석 (중랑구 평균 거래가/거래량)

**무슨 일이 있었나:** "중랑구의 평균 거래가와 거래량을 정리해줘"라는 요청에 따라 `pandas`로 `gu == '중랑구'` 필터링, `deal_type`(매매/전세/월세)별로 그룹을 나눠 평균을 계산했습니다.

**AI가 한 일과 목적:** 단순히 전체 평균을 내지 않고, `deal_type`별로 나눈 이유는 매매가(`price`)와 전세 보증금(`deposit`)이 단위·의미가 다른 값이기 때문입니다. 이걸 섞어서 평균 내면 의미 없는 숫자가 나옵니다. 즉 "계산하기 전에 데이터의 의미를 파악하는 것"이 핵심 작업이었습니다.

**사람이 했다면:**
- `pandas.read_csv()`, `DataFrame` 불리언 인덱싱(`df[df['gu']=='중랑구']`), `.groupby()` 또는 `.mean()` 사용법을 알아야 함
- 그보다 먼저 "이 컬럼들이 서로 다른 의미를 가진 값이니 나눠서 봐야 한다"는 도메인 판단(전세는 매매와 다른 개념)을 스스로 내려야 함 — 이건 프로그래밍 지식보다 데이터 이해력의 문제
- `NaN`(빈 값) 처리: 매매 row에는 `deposit`이 비어있고, 전세 row에는 `price`가 비어있는데, `.mean()`이 자동으로 `NaN`을 무시해준다는 pandas의 동작 원리를 알아야 실수하지 않음

### 2-3. `analyze.py`로 저장 → 표준 라이브러리로 재작성

**무슨 일이 있었나:** 분석 코드를 `code/analyze.py`에 저장한 뒤, "pandas 없이 표준 라이브러리만 써서 다시 짜달라"는 요청에 따라 `csv.DictReader`와 리스트 컴프리헨션으로 같은 결과를 재현했습니다.

**AI가 한 일과 목적:** pandas의 `.mean()`을 직접 손으로 푼 것입니다 — `sum(값들) / len(값들)`. 목적은 두 가지였습니다: (1) 외부 패키지 설치 없이 어디서든 실행 가능하게 만들기, (2) pandas가 내부적으로 하는 일(필터링 → 그룹화 → 평균)을 표준 문법으로 눈에 보이게 풀어써서 학습 자료로 쓸 수 있게 하기.

**사람이 했다면:**
- `csv.DictReader`가 각 행을 `{컬럼명: 값}` 딕셔너리로 돌려준다는 것, 모든 값이 문자열로 들어오므로 `float()`으로 직접 변환해야 한다는 점을 알아야 함 (pandas는 이걸 자동으로 숫자 타입으로 추론해줌)
- 리스트 컴프리헨션(`[x for x in rows if 조건]`)으로 필터링을 직접 구현
- 빈 문자열(`''`)을 `float()`에 넣으면 에러가 나므로 `if row['price']` 같은 방어 코드를 직접 챙겨야 함 — pandas라면 신경 안 써도 되는 부분을 사람이 직접 처리해야 함
- 즉, pandas 없이 짜는 것은 "더 많은 것을 직접 손으로 챙겨야 한다"는 트레이드오프를 몸으로 익히는 과정

### 2-4. 단위 환산 (만원 → 억원)

**무슨 일이 있었나:** 매매가/전세 보증금을 만원 단위에서 억원 단위로 바꿔달라는 요청.

**AI가 한 일과 목적:** `평균값(만원) / 10000` 계산을 코드에 반영. 이건 계산 자체보다 "1억 = 10,000만원"이라는 한국 화폐 단위 상식을 코드 로직에 정확히 매핑하는 작업이었습니다.

**사람이 했다면:** 단위 환산 실수는 흔한 버그의 원인(예: `/1000`으로 잘못 쓰는 실수)이므로, 계산 후 결과값이 상식적으로 말이 되는지(억원 단위 아파트값이 몇 억이 맞는지) 검산하는 습관이 필요합니다.

### 2-5. `base.py` 작성 → 실행 테스트 → 컬럼 매핑

**무슨 일이 있었나:** "자치구명, 법정동명, 건물명..." 같은 한글 컬럼명을 가진 범용 CSV 읽기 코드를 요청받아 작성했는데, 실제로 그 컬럼명을 가진 파일이 프로젝트 폴더에 없었습니다. 실행 테스트를 해보니 `FileNotFoundError`가 발생했고, 이후 기존 CSV(`seoul-apt-latest.csv`, 영문 컬럼명)에 매핑하는 방식으로 수정했습니다.

**AI가 한 일과 목적:** 코드를 작성만 하고 끝내지 않고 실제로 실행해서 에러를 확인한 뒤 정직하게 보고했습니다("실행이 안 됩니다, 이유는 이것입니다"). 이후 사용자가 "기존 파일에 매핑해줘"라고 결정하자, `COLUMN_MAP` 딕셔너리(`{'자치구명': 'gu', ...}`)를 만들어 실제 컬럼명(영문) → 원하는 이름(한글)으로 변환하는 어댑터 계층을 추가했습니다. 원본 데이터에 없는 `건축년도`는 빈 값으로 채우되, 그 사실을 숨기지 않고 사용자에게 명시했습니다.

**사람이 했다면:**
- 코드를 작성한 뒤 반드시 실행해서 검증하는 습관이 필요 — "문법적으로 맞아 보인다"와 "실제로 동작한다"는 다름
- 두 CSV의 컬럼 구조가 다를 때 이를 억지로 끼워 맞추지 않고, 매핑 테이블을 따로 만들어 관리하는 설계 감각이 필요 (하드코딩으로 곳곳에 흩어놓으면 나중에 유지보수가 어려워짐)
- 없는 데이터를 빈 값으로 채울 때는 반드시 그 사실을 기록해두지 않으면, 나중에 "왜 건축년도가 다 비어있지?"라는 혼란을 겪게 됨

### 2-6. 분석 로직 확장 (자치구 간 "갭" 비교)

**무슨 일이 있었나:** "매매 평균가 - 전세 평균 보증금이 가장 큰 자치구 top 3(이후 top 5로 변경)"를 구하는 로직으로 `analyze.py`를 완전히 새로 작성했습니다. `collections.defaultdict`로 자치구별 리스트를 모으고, 정렬(`sorted`)로 순위를 매겼습니다.

**AI가 한 일과 목적:** 이전 단계들이 "하나의 구를 깊게 보는 것"이었다면, 이 단계는 "25개 자치구를 한 번에 비교하는 것"으로 분석의 스케일을 넓힌 것입니다. `defaultdict(list)`를 쓴 이유는 25개 구 각각에 대해 리스트를 미리 만들어두지 않아도 자동으로 빈 리스트가 생기게 하기 위함이고, `sorted(..., key=..., reverse=True)`는 "차이값이 큰 순"이라는 정렬 기준을 명시적으로 표현하기 위함이었습니다. 이후 사용자가 수식 방향(전세-매매 → 매매-전세)과 범위(top3 → top5)를 바꿔달라고 하자, 계산식 한 줄과 슬라이싱 숫자만 수정해 대응했습니다.

**사람이 했다면:**
- `dict`의 기본 동작(없는 키에 접근하면 `KeyError`)과 `defaultdict`가 이를 어떻게 해결하는지 이해해야 함
- `sorted()`의 `key` 매개변수에 함수(또는 람다)를 넘겨서 "무엇을 기준으로 정렬할지" 지정하는 법을 알아야 함
- "TOP N"을 구할 때 정렬 후 슬라이싱(`ranked[:5]`)하면 된다는 패턴, 반대로 "BOTTOM N"은 `ranked[-5:]`로 구한다는 것을 알아야 함
- 요구사항이 바뀔 때(수식 방향, 개수) 코드의 어느 부분만 고치면 되는지 파악하는 것 자체가 코드를 "구조화해서" 짜야 하는 이유

### 2-7. 분석 결과를 `index.html`에 반영

**무슨 일이 있었나:** 코드 실행 결과(top5, bottom5 표와 중랑구 순위)를 실제 웹페이지의 HTML `<table>` 태그로 옮겨 적었습니다.

**AI가 한 일과 목적:** 코드 실행 결과는 터미널에만 존재하는 "숫자"이고, 웹페이지는 사람이 브라우저로 보는 "문서"입니다. 이 둘은 자동으로 연결되지 않으므로, 계산된 숫자를 HTML 표 형식(`<tr><td>...</td></tr>`)으로 직접 옮겨 적는 "번역" 작업이 필요했습니다. 이 과정에서 중랑구가 25개 구 중 몇 위인지 별도로 계산해서 표에 순위 컬럼을 추가하는 등, 단순 복사가 아니라 읽는 사람이 맥락을 이해하기 쉽도록 정보를 보강했습니다.

**사람이 했다면:**
- HTML의 표 구조(`<table>`, `<tr>`, `<th>`, `<td>`)를 알아야 함
- 코드 결과와 웹페이지 내용이 자동으로 동기화되지 않는다는 것을 이해해야 함 — 나중에 분석 코드를 다시 돌려서 숫자가 바뀌면, HTML도 사람이 직접 다시 고쳐야 함 (이번 프로젝트 규모에서는 이 방식이 간단하지만, 데이터가 자주 바뀌는 경우라면 템플릿 엔진이나 스크립트로 HTML을 자동 생성하는 방법을 고려해야 함)

### 2-8. Git/GitHub 업로드

**무슨 일이 있었나:** 로컬 폴더를 git 저장소로 초기화하고, GitHub의 기존 저장소(`sqwogh-web/sesac-web`, 이미 README.md가 있는 상태)와 연결해 push했습니다. 두 저장소의 커밋 역사가 서로 무관했기 때문에 `--allow-unrelated-histories` 옵션으로 병합했습니다.

**AI가 한 일과 목적:** `git init` → `git add` → `git commit` → `git remote add origin` → `git push` 순서로 진행했고, 중간에 `git push`가 거절되자(원격에 이미 커밋이 있어서) 바로 강제로 덮어쓰지 않고 `git fetch`로 원격 내용을 먼저 확인한 뒤, 사용자의 기존 README를 보존하는 방식(병합)을 선택했습니다. 또한 `.gitignore`를 만들어 `__pycache__` 같은 불필요한 캐시 파일이 저장소에 올라가지 않도록 걸렀습니다.

**사람이 했다면:**
- git의 기본 흐름(작업 디렉토리 → 스테이징 → 커밋 → 원격 저장소)을 알아야 함
- `git push`가 거절됐을 때 무작정 `git push --force`로 덮어쓰면 원격에 있던 다른 사람(또는 과거의 나)의 작업을 잃어버릴 수 있다는 위험성을 알아야 함 — 항상 먼저 `git fetch`/`git log`로 원격 상태를 확인하는 습관 필요
- `git config user.name`, `git config user.email` 설정이 커밋에 필요하다는 것, 그리고 GitHub의 `노이름@users.noreply.github.com` 형식의 이메일을 쓰면 실제 이메일을 공개하지 않고도 커밋할 수 있다는 실용적인 팁
- GitHub CLI(`gh`)가 없다면, GitHub 웹사이트에서 Personal Access Token을 발급받아 `git remote`의 URL에 포함시키거나 Credential Manager를 통해 인증하는 방법을 알아야 함

### 2-9. GitHub Pages 배포

**무슨 일이 있었나:** GitHub API(`gh api repos/.../pages`)를 호출해 `main` 브랜치 루트를 GitHub Pages 소스로 지정했습니다.

**AI가 한 일과 목적:** GitHub 웹사이트의 Settings → Pages 메뉴에서 클릭으로 하는 설정을, API 호출로 대신 수행했습니다. 이후 빌드가 완료될 때까지 상태를 주기적으로 확인(polling)해서 배포가 실제로 끝났는지 검증한 뒤에 결과를 보고했습니다.

**사람이 했다면:**
- GitHub 저장소 페이지에서 `Settings → Pages` 로 들어가 `Branch: main`, `Folder: / (root)`를 선택하고 저장하는 몇 번의 클릭이면 충분합니다 — 이 부분은 사실 API보다 웹 UI가 더 직관적입니다
- 배포에는 보통 1~2분이 걸린다는 것을 알고 있어야 "왜 아직 안 보이지?"라고 당황하지 않음

### 2-10. 외부 디자인 시스템 참고해 리디자인

**무슨 일이 있었나:** `wanteddev/montage-web`(원티드의 오픈소스 디자인 시스템) 저장소를 참고해서 `index.html`의 색상/타이포그래피/여백/그림자를 다시 디자인했습니다.

**AI가 한 일과 목적:** 이 디자인 시스템은 React 컴포넌트 라이브러리라서 그대로 가져다 쓸 수 없었기 때문에, `gh api`로 저장소의 소스 코드(색상 값이 정의된 `.ts` 파일들: `blue.ts`, `neutral.ts`, `semantic/index.ts`, 타이포그래피 크기가 정의된 `style.ts` 등)를 직접 읽어서 그 안에 적힌 색상 코드(`#0066FF` 등), 글자 크기, 여백 값, 그림자 값을 추출했습니다. 이 값들을 CSS 변수(`--color-primary-normal` 등)로 옮겨 담아 현재 정적 HTML 페이지에 적용했습니다. 이후 브라우저로 실제 렌더링 결과를 확인하다가, 좁은 화면에서 표가 부자연스럽게 줄바꿈되는 문제를 발견해 가로 스크롤 컨테이너를 추가해 고쳤습니다.

**사람이 했다면:**
- "디자인 시스템을 참고한다"는 것이 컴포넌트 코드를 복사-붙여넣기 하는 게 아니라, 그 안에 담긴 디자인 토큰(색상표, 글자 크기 체계, 여백 단위, 그림자 값 등)을 읽어내서 내 프로젝트의 기술 스택(순수 HTML/CSS)에 맞게 다시 표현하는 작업이라는 것을 이해해야 함
- GitHub 저장소 구조를 탐색하는 능력(어느 폴더에 어떤 종류의 파일이 있을지 추측하고 찾아 들어가는 것)이 필요 — 이번 경우 `packages/wds-theme/src/theme` 아래에 색상이, `packages/wds/src/components/typography` 아래에 글자 크기가 있었음
- CSS 변수(`:root { --color: ...; }`)를 써서 디자인 값들을 한 곳에 모아두면, 나중에 색상을 바꿀 때 CSS 여러 곳을 찾아다니지 않고 변수 값 하나만 바꾸면 된다는 유지보수 이점을 알아야 함
- 코드를 다 쓴 뒤 반드시 실제 브라우저(특히 좁은 화면)에서 눈으로 확인해야 표가 깨지는 것 같은 문제를 발견할 수 있음 — "코드가 에러 없이 실행된다"와 "화면이 의도대로 보인다"는 다른 문제

### 2-11. 페르소나 인터뷰로 PRD 작성하기

**무슨 일이 있었나:** 여기서부터는 새로운 산출물인 "부동산 대시보드"를 만드는 작업이 시작됩니다. 그런데 바로 코드를 짜지 않고, 먼저 **인터뷰를 통해 PRD(제품 요구사항 문서)를 작성**했습니다. 사용자는 "신혼부부"라는 가상의 인물(페르소나)을 가정하고 그 입장에서 질문에 답했습니다.

**AI가 한 일과 목적:** 5라운드에 걸쳐 순차적으로 질문했고, 각 라운드는 앞선 답변을 반영해 다음 질문을 조정했습니다.

1. 누가 쓰는가 (페르소나) → 신혼부부
2. 무엇을 결정하고 싶은가 + 예산 규모 → "가격 대비 살기 좋은 곳 찾기", 예산은 직접 입력
3. 어떤 지표로 판단할까 + 어떤 화면으로 볼까 → 면적/거래량/전세가율, 랭킹 표 + 막대그래프
4. 면적 조건과 상세 화면 → 면적도 직접 선택, 구 클릭 시 12개월 추이
5. 디자인 톤과 사이트 내 위치 → 기존 Montage 스타일 유지, 별도 페이지

한 번에 모든 걸 묻지 않고 단계적으로 좁힌 이유는, **앞 답변에 따라 뒤에 물어야 할 것이 달라지기 때문**입니다. 예를 들어 페르소나가 "투자자"였다면 3번에서 물었을 지표가 완전히 달랐을 것입니다.

또 중요한 판단이 하나 있었습니다. 사용자가 "가격 대비 살기 좋은 곳"이라고 답했을 때, AI는 곧바로 진행하지 않고 **"우리 데이터에는 학군·교통 정보가 없다"**는 사실을 먼저 알렸습니다. 그리고 가진 데이터(가격·면적·거래량)만으로 '살기 좋음'을 표현할 방법을 선택지로 제시했습니다. 원하는 것과 가능한 것 사이의 간극을 초반에 드러낸 것입니다.

마지막으로, 질문 도중 실제 CSV를 열어 데이터 기간(2025-07~2026-06, 12개월)과 면적 분포(중앙값 77.8㎡)를 확인한 뒤 그 숫자를 질문에 넣었습니다. 추측이 아니라 실제 값을 근거로 물어야 답변이 현실적이 되기 때문입니다.

**사람이 했다면:**
- **코드를 짜기 전에 문서를 쓰는 습관**이 필요합니다. 초보자가 가장 흔히 하는 실수는 "일단 만들면서 생각하자"인데, 만들다 보면 방향이 계속 바뀌어 이미 짠 코드를 버리는 일이 반복됩니다
- 요구사항을 물을 때 **"어떻게 만들까"(기술)와 "무엇을 만들까"(기능)를 분리**해야 합니다. 이번 인터뷰는 철저히 후자만 다뤘고, 기술 선택은 개발 단계로 미뤘습니다
- **범위를 줄이는 질문을 반드시 넣어야 합니다.** "있으면 좋은 것"을 다 넣으면 아무것도 완성되지 않습니다. 이번에는 지도·동별 분해·실거래 목록을 명시적으로 "이번 범위 밖"으로 밀어냈습니다
- **데이터의 한계를 먼저 확인**해야 합니다. 원본 CSV가 27.6MB라 브라우저에서 그대로 읽을 수 없다는 사실을 기획 단계에서 발견했기 때문에, "Python으로 미리 집계한 작은 JSON을 만들어 쓴다"는 방침을 PRD에 넣을 수 있었습니다. 이걸 개발 도중에 발견했다면 구조를 다시 짜야 했을 것입니다

> **참고 — 모델을 바꿔가며 작업하기**
> 이번 세션에서는 인터뷰는 Opus(더 깊게 추론하는 모델), 개발은 Sonnet(빠르고 코드에 강한 모델)으로 나눠 진행했습니다. 기획처럼 "무엇을 물어야 할지 스스로 판단해야 하는 일"과 개발처럼 "정해진 것을 정확히 구현하는 일"은 성격이 다르기 때문입니다. 도구를 일의 성격에 맞춰 고르는 것도 작업의 일부입니다.

### 2-12. 데이터 사전 집계 (`code/build_data.py`)

**무슨 일이 있었나:** PRD 6장에서 정한 대로, 27.6MB짜리 원본 CSV를 브라우저가 직접 읽지 않고, Python으로 미리 계산해서 훨씬 작은 JSON 파일(`data/dashboard_data.json`, 약 520KB)을 만들었습니다.

**AI가 한 일과 목적:** `자치구 × 거래유형 × 월 × 면적구간(10㎡ 단위)`이라는 4중 분류로 데이터를 묶고, 각 칸마다 "건수, 가격 합계, 면적 합계"라는 최소한의 숫자 3개만 저장했습니다. 개별 거래 32만 건을 다 보내는 대신, 나중에 웹페이지가 그때그때 원하는 예산·면적 범위에 맞춰 **필요한 칸들만 골라 합산**할 수 있도록 미리 "재료 손질"을 해둔 것입니다. 예를 들어 "60~85㎡"를 선택하면 60·70·80㎡ 세 칸을 더하면 되는 구조입니다.

**사람이 했다면:**
- 원본 데이터를 그대로 웹에 올리면 안 되는 이유(파일 크기, 브라우저 메모리, 로딩 속도)를 알아야 하고, 그 대안으로 "미리 집계해서 작게 만든다"는 발상을 스스로 떠올려야 합니다
- 집계 단위를 너무 세밀하게 하면(예: 1㎡ 단위) 파일이 다시 커지고, 너무 크게 하면(예: 구별 평균 하나만) 나중에 사용자가 면적을 바꿀 때 대응할 수 없습니다 — "나중에 뭘 할 수 있어야 하는가"를 먼저 정하고 그에 맞는 집계 단위를 거꾸로 설계하는 능력이 필요합니다
- `딕셔너리 안에 딕셔너리 안에 딕셔너리`(중첩 구조)를 다루는 코드에 익숙해야 합니다. `data.setdefault(key, {})` 같은 패턴으로 "키가 없으면 만들고, 있으면 그대로 쓴다"는 처리를 직접 짤 수 있어야 합니다

### 2-13. `dashboard.html` 개발

**무슨 일이 있었나:** PRD의 MVP 스코프 그대로 — 예산·면적 입력, 자치구 랭킹 표+막대그래프, 구 클릭 시 12개월 추이 그래프 — 를 하나의 페이지로 구현했습니다. 차트는 Chart.js(CDN)를 사용했고, 나머지는 순수 JavaScript로 작성했습니다.

**AI가 한 일과 목적:** 계산 로직(자바스크립트)과 화면(HTML/CSS)을 분리해서 짰습니다. 사용자가 예산이나 면적을 바꿀 때마다 `render()` 함수 하나가 다시 실행되며 표와 그래프를 모두 갱신하는 구조입니다. 지표 계산식은 PRD 3장에 정의된 그대로 코드로 옮겼습니다: `내 예산으로 얻는 면적 = 예산 ÷ (선택 면적대의 평균 평단가)`, `전세가율 = 평균 전세보증금 ÷ 평균 매매가`. 거래가 아예 없는 구(예산이 너무 낮아 매물이 없는 경우)는 회색으로 흐리게 표시해 "0등"이 아니라 "비교 불가"임을 구분했습니다. 또한 기존 `index.html`과 색상·글자체·카드 스타일을 동일한 CSS 변수로 맞춰, 두 페이지가 한 사이트처럼 보이게 했습니다.

**사람이 했다면:**
- **입력값이 바뀔 때마다 화면 전체를 다시 계산해서 그리는 패턴**(선언형 렌더링)에 익숙해야 합니다. "이 값이 바뀌면 저 부분만 고친다"는 식으로 여기저기 땜질하면 코드가 금방 꼬입니다
- **HTML `<canvas>` 요소 위에 차트 라이브러리(Chart.js)를 얹는 법**을 알아야 합니다: CDN에서 스크립트를 불러오고, `new Chart(캔버스, {type, data, options})` 형태로 설정해야 합니다
- 값이 없는 경우(0으로 나누기, 거래가 없는 구)를 미리 걸러내지 않으면 `Infinity`나 `NaN`이 화면에 그대로 노출되는 실수를 하기 쉽습니다 — 조건문으로 "데이터 없음" 상태를 명시적으로 처리해야 합니다
- **fetch로 JSON을 비동기로 불러오는 것**(`await fetch(...).then(res => res.json())`)과, 그 데이터가 도착하기 전까지 화면이 비어있는 상태를 어떻게 처리할지 알아야 합니다

### 2-14. 반응형/인터랙션 검증

**무슨 일이 있었나:** 코드를 다 짠 뒤, 실제 브라우저에서 데스크톱 화면과 모바일 폭(375px) 두 가지로 열어보고, 예산·면적 값을 바꿔가며 표/그래프가 실시간으로 갱신되는지, 구를 클릭하면 추이 그래프가 나타나는지 하나씩 확인했습니다. 콘솔에 에러가 없는지도 확인했습니다.

**AI가 한 일과 목적:** "코드가 에러 없이 실행된다"와 "의도한 대로 동작한다"는 다른 문제이기 때문에, 완료를 보고하기 전에 반드시 눈으로 확인하는 절차를 거쳤습니다. 특히 이번 페이지는 사용자 입력(예산, 면적)에 따라 화면이 실시간으로 바뀌는 **상호작용형** 페이지라서, 정적인 페이지보다 검증해야 할 경우의 수가 많습니다(입력값을 바꿨을 때, 클릭했을 때, 화면이 좁을 때 등).

**사람이 했다면:**
- 코드 리뷰만으로는 안 되고, 실제로 브라우저를 열어 값을 입력해보고 클릭해보는 수동 테스트가 필요합니다
- 브라우저 개발자 도구의 콘솔 탭에서 에러 메시지를 확인하는 습관이 필요합니다 — 화면은 멀쩡해 보여도 콘솔에 에러가 쌓이고 있는 경우가 흔합니다
- 모바일 폭을 흉내 내서 확인하는 습관(반응형 디자인 검증)이 필요합니다 — 데스크톱에서만 확인하면 실제 스마트폰 사용자가 보는 화면의 문제를 놓치게 됩니다

---

## 3. 전체를 관통하는 세 가지 습관

이번 세션에서 반복적으로 등장한, 초보자가 특히 눈여겨볼 만한 습관입니다.

1. **가정하지 않고 확인한다** — 한글이 깨져 보일 때 "파일이 손상됐다"고 단정하지 않고 바이트를 직접 열어봤고, `base.py`를 작성한 뒤 실제로 실행해서 에러를 확인했습니다.
2. **되돌릴 수 없는 작업 전에는 원격 상태를 먼저 본다** — `git push`가 거절됐을 때 강제로 덮어쓰지 않고 `fetch`로 먼저 원격 내용을 확인했습니다.
3. **결과를 눈으로 검증한다** — 계산 결과가 상식과 맞는지(억원 단위), 웹페이지가 실제로 브라우저에서 의도대로 보이는지(반응형 표) 매번 확인 후에 완료를 보고했습니다.

이 세 가지는 프로그래밍 언어나 도구가 바뀌어도 계속 유효한 원칙입니다.
